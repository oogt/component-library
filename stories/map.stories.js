import React from "react";

import { StoreLocator, GoogleMaps } from "../components/maps";

export default { title: "Map" };

export const AsStoreLocator = () => <StoreLocator />;

export const AsGoogleMaps = () => <GoogleMaps />;
